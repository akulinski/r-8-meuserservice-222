/*
package com.akulinski.r8meservice.domain;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;
import com.akulinski.r8meservice.web.rest.TestUtil;

public class CommentXProfileTest {

    @Test
    public void equalsVerifier() throws Exception {
        TestUtil.equalsVerifier(CommentXProfile.class);
        CommentXProfile commentXProfile1 = new CommentXProfile();
        commentXProfile1.setId(1L);
        CommentXProfile commentXProfile2 = new CommentXProfile();
        commentXProfile2.setId(commentXProfile1.getId());
        assertThat(commentXProfile1).isEqualTo(commentXProfile2);
        commentXProfile2.setId(2L);
        assertThat(commentXProfile1).isNotEqualTo(commentXProfile2);
        commentXProfile1.setId(null);
        assertThat(commentXProfile1).isNotEqualTo(commentXProfile2);
    }
}
*/
